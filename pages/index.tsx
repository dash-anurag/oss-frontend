import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Open Source School</title>
      </Head>
      {/* Header */}
    </div>
  );
};

export default Home;
